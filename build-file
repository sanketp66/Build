stage('Build') {
            steps {
                script {
                    // Build commands
                    sh 'echo "Building started"'
                    sh 'bash build_script.sh'  // Example build script
                    sh 'echo "Building completed"'
                }
            }
        }
    }
}
